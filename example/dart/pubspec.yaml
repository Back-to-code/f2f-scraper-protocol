name: example
description: Dart gRPC scraper example
publish_to: none

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  async: ^2.2.0
  grpc: ^3.1.0
  protobuf: ^2.0.0
  scraperProtocol:
    path: ../../

dev_dependencies:
  lints: ^2.0.0
